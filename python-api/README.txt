//Equipo, aquí está el modelo y la API. Si ejecutan `pip install -r requirements.txt` y luego `python app.py`, 
//tendrán el servicio de predicción corriendo en `localhost:5000`. 
//Ya pueden empezar a hacerle llamadas POST desde Java con el formato JSON que acordamos